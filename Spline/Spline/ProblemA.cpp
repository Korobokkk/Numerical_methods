#include "pch.h"
#include "problemA.h"

void ProblemA::CalculateF(int key) {
    double function;
    switch (key) {
        case 1:
            function =

        default:
            break;
    }


    for (int i = 0; i < n; i++) {
        ThroughtCoef.f.push_back();
    }

}

void ProblemA::CountCoef(){


    ThroughtCoef.a.push_back();

    ThroughtCoef.c.push_back(s_double_dif_a);
    for (int i = 1; i < this->n - 1; i++) {
        
    }

    ThroughtCoef.c.push_back(s_double_dif_b);
}